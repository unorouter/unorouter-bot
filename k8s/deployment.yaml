# Discord bot: SINGLE gateway connection -- never more than 1 replica (duplicate event
# handling otherwise). Recreate strategy keeps that true during rollouts.
apiVersion: v1
kind: Service
metadata:
  name: unorouter-bot
  namespace: services
spec:
  selector: { app: unorouter-bot }
  ports:
    - port: 4000
      targetPort: 4000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unorouter-bot
  namespace: services
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: { app: unorouter-bot }
  template:
    metadata:
      labels: { app: unorouter-bot }
    spec:
      containers:
        - name: bot
          # pinned by this repo's GHCR workflow (deploy job); ArgoCD rolls it out.
          # NEVER set this back to :latest -- a floating tag never changes the manifest, so
          # ArgoCD sees no diff and the deploy silently does not happen.
          image: ghcr.io/unorouter/unorouter-bot:6275793dfe679fb54eb251443b525b3b7f1e2601
          resources:
            requests: { memory: 256Mi, cpu: 50m }
            limits: { memory: 768Mi }
          envFrom:
            - secretRef: { name: bot-env }
          ports:
            - containerPort: 4000
          readinessProbe:
            httpGet: { path: /health, port: 4000 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 4000 }
            initialDelaySeconds: 30
            periodSeconds: 20
